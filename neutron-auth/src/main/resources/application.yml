server:
  port: 9000
  forward-headers-strategy: framework
spring:
  profiles:
    active: dev
  application:
    name: neutron-auth
  jackson:
    time-zone: GMT+8
    date-format: "yyyy-MM-dd HH:mm"
  threads:
    virtual:
      enabled: true
  security:
    oauth2:
      authorizationserver:
        multiple-issuers-allowed: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${datasource.url}:${datasource.port}/${datasource.database}?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&allowMultiQueries=true
    username: ${datasource.username}
    password: ${datasource.password}
    hikari:
      connection-test-query: "SELECT 1"
      minimum-idle: 2
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
  jpa:
    database: mysql
    hibernate:
      naming:
        implicit-strategy: org.springframework.boot.hibernate.SpringImplicitNamingStrategy
  data:
    redis:
      host: ${redis.host}
      port: ${redis.port}
      database: ${redis.database}
      password: ${redis.password}
  config:
    import:
      - nacos:${spring.application.name}.yml?group=${spring.profiles.active}&refreshEnable=true
      - nacos:neutron-global.yml?group=${spring.profiles.active}&refreshEnable=true
  cloud:
    nacos:
      username: ${nacos.username}
      password: ${nacos.password}
      config:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
        group: ${spring.profiles.active}
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
        namespace: ${nacos.namespace}
        group: ${spring.profiles.active}
---
spring:
  config:
    activate:
      on-profile: dev
    import: file:neutron-auth/.env.development[.yml]
  thymeleaf:
    cache: false
logging:
  level:
    org.hibernate.SQL: trace
    org.hibernate.orm.jdbc.bind: trace
    io.github.hejun.neutron.auth: trace
---
spring:
  config:
    activate:
      on-profile: prod
    import: file:.env.production[.yml]